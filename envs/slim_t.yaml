name: xsuite-env
channels:
  - conda-forge
dependencies:
  - python=3.14
  - meson
  - h5py
  - pytest
  - pytest-mock
  - cmake
  - pip
  - pip:
      - numpy 
      - scipy 
      - matplotlib 
      - cython 
      - ipython 
      - setuptools
      - pyklu
